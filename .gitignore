# Dev tool dependencies (installed locally, not needed in repo)
dev/node_modules/

# Test images (generated by AI, imported locally)
dev/*.png

# Generated element PNGs (created by generate_elements.py, not committed)
dev/generated/

# Local API keys — NEVER commit
dev/.env
